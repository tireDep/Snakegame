public enum SnakeSegmentType
{
    Head,
    Body,
    BodyCorner,
    Tail
}

public enum GameState
{
    Ready,
    Playing,
    GameOver,
    Clear
}

public enum BoardSize
{
    Small,
    Medium,
    Large,
    ExtraLarge
}